(ns emissary.core-test
  (:require [clojure.test :refer :all]
            [emissary.core :refer :all]))


(deftest dispatcher-test
  (testing "dispatcher"
    (let [effects (atom :unchanged)
          ])))
